# wrsrcli — Progress

Log entry at the start and end of each phase in `EXECUTION-PLAN.md`, per
`REPO-STRUCTURE.md`. An end-of-phase entry is written only once that
phase's "Verify" step has been run and passed.

|Date|Time|Note|
|---|---|---|
|2026-09-19|23:22|Phase 0 started.|
|2026-09-19|23:23|Phase 0 finished. Verify passed: `wrsrcli --help` lists all 10 subcommands. Total development time this phase: 00:01.|
|2026-09-19|23:23|Phase 1 started.|
|2026-09-19|23:24|Phase 1 finished. Verify passed: api store/inspect/remove cycle; `path --auto-detect` resolved game path via appmanifest installdir to `...\common\SovietRepublic`; `path --game` override and directory validation. D-002 recorded. Total development time this phase: 00:01.|
|2026-09-19|23:24|Phase 2 started.|
|2026-09-19|23:26|Phase 2 finished. Verify passed: `.acf` fields hand-checked against raw for item 3621284903; SPEC 2.2 sample 3779842468 reproduced field-for-field; all 20 `workshopconfig.ini` files parse with `$ITEM_ID` matching folder name. Total development time this phase: 00:02.|
|2026-09-19|23:26|Phase 3 blocked pending decisions on open item #3 (manifest.json location) and the item-without-workshopconfig.ini case. Awaiting input.|
|2026-09-19|23:40|Phase 3 started (unblocked; D-003, D-004, D-005 recorded and SPEC 2.2/4.1/6 updated).|
|2026-09-19|23:40|Phase 3 finished. Verify passed: `wrsrcli scan` wrote 21 entries to `%APPDATA%\wrsrcli\manifest.json`; item 3780739284's `date_updated`/`date_touched` hand-checked against raw `.acf`; item 3780813529 included with null fields plus warning per D-005. Total development time this phase: 00:00.|
|2026-09-19|23:41|Phase 4 started (D-006 recorded; SPEC 4.2 column list updated).|
|2026-09-19|23:44|Phase 4 finished. Verify passed: verbatim no-API message; save prompt incl. ENTER-to-Documents default; collision prompt both branches with `WRSR Assets YYYY-MM-DD HH-mm.html` timestamp; HTML has zero external references; search, type filter, text sort (asc/desc) and numeric date sort all exercised in a browser against the real 21-item dataset. Total development time this phase: 00:03.|
|2026-09-19|23:44|Phase 5 blocked: requires a Steam Web API key, which is not set. Awaiting input.|
|2026-09-19|23:45|Phase 5 started (code-only, by agreement; verification deferred until a key is available).|
|2026-09-19|23:47|Phase 5 **code complete, UNVERIFIED — phase NOT finished.** Both endpoints implemented, enrichment wired into `output-table`, D-007 recorded. Offline checks passed (8-column API render, author fallback, D-007 degradation, key absent from output, no-API regression). The live `GetPublishedFileDetails`/`GetPlayerSummaries` calls have never been executed — Phase 5's Verify step remains outstanding and requires a stored API key. Development time so far this phase: 00:02.|
|2026-09-19|23:48|Phase 6 started (D-008 recorded, closing open item #4; SPEC 3 and 6 updated).|
|2026-09-19|23:50|Phase 6 finished. Verify passed: verbatim prompt; only ENTER proceeds and a non-empty reply downloads nothing; `--path` without `--install` and bare `steamcmd` are usage errors; the default path short-circuits, leaving the existing install untouched. Installed to a throwaway path — 774,825 bytes downloaded, self-bootstrapped to client version 1788292693, then downloaded workshop item 3780739284 anonymously (712,541 bytes, matching the `.acf` size) with all 5 files SHA256-identical to the installed copy. Total development time this phase: 00:02.|
|2026-09-19|23:51|Phases 7 and 8 started together (D-009 closing open item #5, and D-010 for copy-overlap mechanics; SPEC 4.3, 5 and 6 updated).|
|2026-09-19|23:52|Phases 7 and 8 finished. Verify passed: 22 checks in a sandbox (APPDATA redirected, disposable game/workshop fixtures, real config and game folder never touched). Covered the conflict prompt, `*`/directory/file sources, `[GAME]` and `[WORKSHOP]/{id}/` destinations, removals of files and folders, absent targets warning without failing, backup-before-write for both actions, `restore` returning a destination's file byte-for-byte, `rollback` undoing both overwrites and removals, the multi-generation prompt with ENTER-cancel and re-prompt on bad input. Found and fixed a same-second run-stamp collision that would have let one generation overwrite another's backups. Total development time this phase: 00:01.|
|2026-09-19|23:53|Phase 9 started (D-011 for import-list tracking, since SPEC 5's backup schema cannot describe a copy's source).|
|2026-09-19|23:54|Phase 9 finished. Verify passed: 14 sandboxed checks — import registered with a verbatim copy of the list, clean state flags nothing, a simulated Steam revert is detected and names the right file, `manual-rerun` replays from the stored copy and restores the expected state while backing up what it overwrote, and an empty registry is handled gracefully. Found and fixed a false positive that flagged every import the moment it completed (D-012). Phase 7-8 suite re-run: no regression. Total development time this phase: 00:01.|
|2026-09-19|23:55|Phase 10 started (D-013 closing open item #6: `v*` tags plus manual dispatch, Windows-only).|
|2026-09-19|23:57|Phase 10 **code complete, verify PARTIAL.** Workflow added; `.exe` built locally with PyInstaller 6.22.3 and confirmed working — `--version`, `--help` listing all 10 subcommands, bundled `winreg` via `path --auto-detect`, bundled PyYAML via a full `import`, run outside the repo with PYTHONPATH/PYTHONHOME cleared. The criterion's "on a Windows machine with no Python installed" half is NOT verified and cannot be from this machine. Total development time this phase: 00:02.|
|2026-09-20|00:03|Phase 5 verification resumed. `GetPublishedFileDetails` needs no API key, so that half is now **verified live**: all 21 items returned, file sizes matched the `.acf` with 0 mismatches, and item 3779842468 cross-checked against its real Steam Workshop page — File Size 1.162 MB, Posted 9 Aug 9:27am, Updated 12 Aug 4:23pm, all matching to the minute once the page's UTC-7 rendering is accounted for. Real enrichment rendered through the 8-column API table with no external refs. **Still outstanding:** `GetPlayerSummaries` author-name resolution, which requires a stored key. Expected result for 3779842468: owner 76561198050524085 resolves to `UltimateUniverse`. Development time so far this phase: 00:03.|
|2026-09-20|00:25|Repository review (not a numbered phase). Found: `LICENSE` is Apache-2.0 while `pyproject.toml` declared MIT; import-list *destinations* are not confined to the game/workshop roots though sources are; no tests are committed; `dev/docs/` and `dev/bugs/` were specified but absent; `uninstall` shipped contradicting D-014 with no superseding record. Logged rather than fixed in place.|
|2026-09-20|00:33|B-001 logged: double-clicking the release `.exe` shows an argparse usage error instead of the first-run installer. Reported with a screenshot from a Windows 11 machine using Windows Terminal as its default terminal host.|
|2026-09-20|00:45|Documentation pass. Added `dev/docs/{Install,Use,Import}.md` and a real `README.md`; resolved the licence contradiction in favour of Apache-2.0 and filled in the copyright line; specified `uninstall` in `SPEC.md` §3 and recorded D-015 superseding D-014's exclusion of it; corrected `AGENTS.md`'s subcommand list (10 → 12); repaired `Progress.md`, whose blank lines had split one table into five; removed the stray `dev/REPO-STRUCTURE.md.backup`.|
|2026-09-20|00:52|B-001 fixed. Double-click detection now identifies the parent process (`explorer.exe`) rather than counting processes attached to the console, which Windows Terminal made meaningless. Old test kept as a fallback. Verify passed: 13 checks over the decision logic, `parent_process_name()` cross-checked against `Win32_Process`, terminal behaviour unchanged. End-to-end double-click of a release build remains outstanding.|
|2026-09-20|01:18|First-run screen reworded to the requested text, with the title line and the `Press ENTER to install...` prompt in rusty red (`#b7410e`). Colour is raw ANSI with `ENABLE_VIRTUAL_TERMINAL_PROCESSING` switched on via `ctypes` — no new dependency — and is dropped entirely when the console cannot be confirmed to handle escapes, when `NO_COLOR` is set, or when stdout is not a terminal. Only ENTER now proceeds, matching D-008. D-016 recorded; SPEC 3 and `dev/docs/Install.md` updated. Verify passed: 17 checks, including the screen matching the requested text verbatim.|
|2026-09-20|01:02|B-001's first fix did not work; reporter saw the identical error from a rebuilt release. Real cause found by running a frozen probe: PyInstaller `--onefile` is two processes, so our immediate parent is always our own bootloader and the console always has two processes attached. Neither original signal could ever have worked in a release build, and the console host was irrelevant. Detection now skips leading generations sharing our executable's name. Verify passed end to end this time: the real `.exe` launched through `explorer.exe` sits at the install prompt, the same binary from a shell still exits 2 with the usage error, plus 17 logic checks. Lesson recorded in B-001: the first fix was verified against the decision logic and an unfrozen process, never against the artifact that was failing.|
|2026-09-20|01:40|Four requested changes. Bare `wrsrcli` now prints the help and exits 0 instead of argparse's usage error (exit code change recorded in D-017); the HTML table's accent moved to the console's rust, `#b7410e` light / `#d9793f` dark, both contrast-checked against their backgrounds at 5.14:1 and 5.76:1; the first-run prompt now colours only the word `ENTER`, with the rest left uncoloured rather than forced white so light-background terminals still work; and `open-web` added, opening `https://wrsr-tools.github.io/` via stdlib `webbrowser`. D-017 recorded; SPEC 3, AGENTS.md's verb list and both user docs updated. Verify passed: 17 first-run checks including colour placement, B-001's 17 detection checks re-run with no regression, bare `wrsrcli` and `open-web` exercised directly.|
|2026-09-20|01:52|First-run banner reads `wrsrcli v0.1` instead of `wrsrcli dev`. The version is major.minor derived from `__version__` rather than typed into the string, so it cannot drift from what the same build reports through `--version`. SPEC 3 and `dev/docs/Install.md` updated to match. Verify passed: 17 first-run checks, the screen still matching the specified text verbatim.|
|2026-09-20|02:34|Start: the `--help` screen is to carry the rust accent instead of the colours it currently shows. Cause established first — `argparse` gained colourised help in Python 3.14 and switches it on by default, and `release.yml` pins `python-version: "3.x"`, so released executables pick it up without any code asking for it. Reproduced under 3.14 directly: five colours, none of them ours.|
|2026-09-20|02:52|The help screen now carries the rust accent on section headings, command names, option strings and the `usage:` prefix, with descriptions left at the terminal's own foreground; Python 3.14's own colourised help is suppressed rather than layered under it. Colour is painted after argparse has laid the text out, because escape sequences count towards the `len()` that positions the help column. D-019 recorded; SPEC 4 and `dev/docs/Use.md` updated. Verify passed: 24 checks on each of Python 3.12, 3.13 and 3.14 — including that stripping every escape from the coloured help reproduces the uncoloured help byte for byte, that only `#b7410e` escapes appear, and that subcommand help inherits the accent; D-017's exit codes re-checked unchanged. D-019 flags that `#b7410e` measures 3.52:1 on Windows Terminal black and fails WCAG AA — left as-is pending a decision, since changing it changes D-016.|
|2026-09-20|16:10|WMLS v1 support. Import lists now declare `version: 1` and carry an `items:` list, with `depends-mandatory:`/`depends-optional:` per item; `*` inside a path is rejected rather than failing later as a missing file. `beta` files (no `version:`) still apply, with a deprecation warning, so stored copies in `imports/` keep replaying. One item is one backup generation, so `restore`/`rollback`/`manual-check` are unchanged, and `manual-rerun` replays only the origin each history entry tracks instead of re-applying a multi-item file once per item it contains. D-018 recorded; SPEC 4.3 and `docs/Import.md` updated. Verify passed: 51 sandboxed checks (APPDATA redirected, disposable game/workshop fixtures) over v1 and beta parsing, 12 malformed-file rejections, a two-item import with dependencies, `only=` replay isolation, and manual-rerun over a multi-item list. **Not verified:** a dependency actually downloading through SteamCMD — that path needs network and an installed SteamCMD.|

Total development time across all phases: 00:18.

All 6 numbered open items in `SPEC.md` §6 are now closed (D-001, D-003,
D-008, D-009, D-013) or deferred by design (#1, the `$TAGS` mapping).
